rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access is through Firebase Admin SDK (server-side)
    // Client-side access is denied
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
